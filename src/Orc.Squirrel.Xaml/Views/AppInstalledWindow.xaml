<catel:DataWindow x:Class="Orc.Squirrel.Views.AppInstalledWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:catel="http://catel.codeplex.com"
                  WindowStartupLocation="CenterScreen" ShowInTaskbar="True" Width="600" Height="400">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Description}" TextWrapping="Wrap" />
        
        <TextBlock Grid.Row="1">
            <Hyperlink Command="{Binding RunApplication}">Run the application now</Hyperlink>
        </TextBlock>
        
        <!-- TODO: make pretty -->
        
    </Grid>

</catel:DataWindow>
